# requirements-gpu.txt
# ---------------------------------------------------------------------------
# ML / GPU dependencies for microservice containers (OCR, NER, Vision).
#
# These are NOT required by the core FastAPI app — they run in separate
# Docker containers (ocr, ner, vision). Install only when running the
# microservices directly on the host:
#
#   pip install -r requirements.txt -r requirements-gpu.txt
# ---------------------------------------------------------------------------

# GPU 显存监控（侧栏 /health/services；NVML）
nvidia-ml-py>=12.535.133,<13.0.0

# HaS Image（YOLO11 微服务 has_image_server.py，端口 8081）
ultralytics>=8.3.0,<9.0.0

# OCR（PaddleOCR-VL 微服务 ocr_server.py）
# 推荐：先安装 GPU 版 Paddle，再装本文件，避免默认装上 CPU 版 paddlepaddle。
# Windows + NVIDIA：在项目根目录执行 scripts\install_paddle_gpu.ps1
# 验证: python -c "import paddle; print(paddle.get_device())"  # 应为 gpu:0
paddleocr[doc-parser]>=2.7.0,<3.0.0
