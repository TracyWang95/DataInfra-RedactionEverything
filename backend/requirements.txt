# FastAPI 框架
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.18

# 数据验证
pydantic>=2.10.0
pydantic-settings>=2.7.0

# 文件处理
python-docx==1.2.0
PyMuPDF==1.27.2.2
Pillow>=11.0.0
pdf2image==1.17.0
pywin32==311; sys_platform == 'win32'  # Windows: 用于处理 .doc 文件

# HTTP 客户端
httpx>=0.28.0

# GPU 显存（侧栏 /health/services；NVML，不依赖 PATH 中的 nvidia-smi）
nvidia-ml-py>=12.535.133
aiofiles>=24.1.0

# HaS Image（YOLO11）和 OCR（PaddleOCR-VL）已拆分为独立微服务：
#   - Dockerfile.vision + requirements-vision.lock（端口 8080 视觉模型）
#   - Dockerfile.ocr    + requirements-ocr.lock   （端口 8081 OCR/HaS Image）
# 主后端不再直接依赖 ultralytics / paddleocr，因此这里不列出。
# 如需本地跑微服务，请分别 pip install -r requirements-vision.lock / requirements-ocr.lock。

# 工具库
PyJWT>=2.8.0
passlib[bcrypt]==1.7.4

# 可观测性
python-json-logger>=2.0.0
prometheus-client>=0.20.0

# 安全（可选：文件加密需要 cryptography，病毒扫描需要 pyclamd + ClamAV daemon）
cryptography>=42.0.0
# pyclamd>=0.4.0  # 按需安装，需配合 ClamAV daemon

# 开发工具
python-dotenv>=1.0.1
pytest>=8.0.0
