# Pinned dependencies for OCR microservice (offline bastion: CUDA 12.6).
# Re-generate: pip freeze > requirements-ocr.lock
#
# Use cu126 wheels to match bastion driver/toolkit (paddle.version.cuda() -> 12.6).
# NVIDIA Container Toolkit injects host CUDA libs at runtime; image only needs libgomp etc.
--extra-index-url https://www.paddlepaddle.org.cn/packages/stable/cu126/
paddlepaddle-gpu==3.3.0
paddleocr[doc-parser]==3.6.0
fastapi==0.136.1
uvicorn==0.35.0
httpx==0.28.1
numpy>=1.24.0
Pillow>=12.2.0
pydantic>=2.0.0
